# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'
use_frameworks!

target 'ACNewsApp' do
  #pod 'Firebase/Core'
  #pod 'Firebase/Auth'
  #pod 'Firebase/Firestore'
end
